## Languages
C — primary language, used for kernel modules, drivers, systems tools (10+ years)
Rust — growing proficiency, used for safe systems programming and tooling
Python — scripting, automation, data processing, AI/ML tooling
Shell (bash/zsh) — daily driver for automation and system administration
Assembly — x86/ARM, used for optimization and hardware interfacing

## Kernel & drivers
Linux kernel module development — loadable kernel modules, device drivers
Character device drivers — cdev interface, ioctl, mmap
USB drivers — URB handling, USB core API
DMA — dma_alloc_coherent, scatter-gather, streaming DMA
PCIe — BAR mapping, MSI interrupts, PCIe config space
Interrupt handling — request_irq, threaded IRQs, softirqs

## Tools
gdb — kernel debugging with KGDB, userspace debugging
perf — CPU profiling, cache analysis, lock contention
ftrace — function tracing, latency analysis
strace/ltrace — system call tracing
valgrind — memory error detection
vim/neovim — primary editor with custom configuration
Makefile — kernel build system, custom build scripts

## What I don't do
GUI frameworks (Qt, GTK, Electron)
Web frontend development
Mobile development (iOS, Android)
Cloud infrastructure management
