## OS
Arch Linux — rolling release, installed from scratch, custom kernel config

## Editor
Neovim with custom configuration — LSP for C/Rust/Python, treesitter highlighting,
telescope for fuzzy finding, no mouse, keyboard-driven workflow

## Terminal
Alacritty — GPU-accelerated, minimal config
tmux — session management, split panes for code + build + logs

## Hardware
[Fill with actual machine specs — CPU, RAM, storage]
Raspberry Pi 5 — used for ARM driver testing and IoT projects

## Workflow
Everything in the terminal. No GUI file manager, no IDE, no browser for development.
Build → test → debug cycle entirely in terminal split panes.
Cross-compilation for ARM targets via custom toolchains.
